<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1>Events</h1>
    <% if user_signed_in? %>
      <%= link_to "New Event", new_event_path, class: "btn btn-primary" %>
    <% end %>
  </div>

  <% if @events.empty? %>
    <p class="text-muted">No events yet.</p>
  <% else %>
    <div class="row">
      <% @events.each do |event| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title"><%= link_to event.name, event %></h5>
              <p class="card-text text-muted"><%= event.start_date.strftime("%B %d, %Y") %></p>
              <p class="card-text"><%= event.location %></p>
              <p class="card-text">
                <small class="text-muted">By <%= link_to event.user.name, user_path(event.user) %></small>
              </p>
              <span class="badge bg-<%= event.published? ? 'success' : event.draft? ? 'secondary' : event.cancelled? ? 'danger' : 'dark' %>">
                <%= event.status.capitalize %>
              </span>
            </div>
            <div class="card-footer">
              <%= link_to "View", event, class: "btn btn-sm btn-outline-primary" %>
              <% if user_signed_in? && (current_user == event.user || current_user.admin?) %>
                <%= link_to "Edit", edit_event_path(event), class: "btn btn-sm btn-warning" %>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>